package /*package*/;

public class /*className*/ {

	public static /*type*/ function(/*arguments*/){
		///*comment*/
		return /*functionCode*/;
	}
	
	private static /*type*/ protectedDiv(/*type*/ x, /*type*/ y) {
		if (y == 0)
			return 0;
		return x / y;
	}

	private static double protectedLog(double x) {
		if (x <= 0)
			return 0;
		return Math.log(x);
	}
}
